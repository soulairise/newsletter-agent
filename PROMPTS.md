# 사용한 프롬프트와 그렇게 쓴 이유

파이프라인은 LLM 을 **네 자리**에서 부릅니다. 각 프롬프트가 무엇을 묻고,
왜 그 문장이 되었는지 적었습니다. 실제 코드는 `graph.py` · `gate_g4.py` 에 있습니다.

프롬프트 안의 `{reader}` `{focus}` `{exclude}` `{topics}` 는 `audience.yaml` 에서 옵니다.
**분야를 바꿀 때 코드가 아니라 이 YAML 을 고칩니다.**

---

## ① 선별 — 예선·본선 (`graph.py: _rank`)

```
당신은 {reader}을 위한 뉴스레터 편집자입니다.
이 중 중요한 {N}건을 고르세요.
반드시 {focus}의 기사만 고르세요.
{exclude} 제외하세요.
같은 사건을 다룬 기사는 하나만 고르고, event 라벨을 같게 붙이세요.
홍보·채용공고·행사안내는 고르지 마세요.

후보:
0. [Yoga Journal] 요가 실력 향상, 어떻게 알 수 있을까요?
1. [Mindful.org] 명상 완벽주의 함정에서 벗어나는 3가지 방법
...
```

**구조화 출력으로 받습니다.**

```python
class Pick(BaseModel):
    index: int = Field(description="후보 목록에서의 번호")
    event: str = Field(description="이 기사가 다루는 사건의 짧은 라벨. 같은 사건이면 반드시 같은 라벨")
    reason: str = Field(description="고른 이유 한 줄")
```

### 왜 이렇게 썼나

- **"몇 점인가"가 아니라 "이 중 몇 건"을 묻습니다.** 독립 채점을 먼저 해 봤더니
  12건에 점수 값이 5개뿐이라 경계에서 자를 근거가 없었습니다. 출력 개수가 고정된
  **정렬 문제**이지 채점 문제가 아닙니다.
- **`event` 라벨을 스키마에 넣은 것**이 핵심입니다. 필드 설명이 곧 지시로 작동해서,
  모델의 내부 판단("이 둘은 같은 사건")이 **우리가 셀 수 있는 값**으로 밖에 나옵니다.
- `reason` 은 탈락 사유를 남기려고 넣었습니다. 이유가 없으면 선별이 잘못됐을 때
  무엇을 고칠지 알 수 없습니다.

---

## ② 취재 — 본문 요약 (`graph.py: draft_one`)

```
당신은 {reader}을 위한 뉴스레터 기자입니다.
topic 은 반드시 다음 중 하나: {topics}
아래 원문만을 근거로 쓰세요. 원문에 없는 사실을 덧붙이지 마세요.
반드시 한국어로 쓰세요.

제목: {title}
원문:
{body[:6000]}
```

```python
class Draft(BaseModel):
    headline: str = Field(description="한국어 헤드라인 한 줄. 40자 이내")
    summary:  str = Field(description="한국어 세 문장 요약. '~합니다'체. 원문에 있는 내용만")
    why:      str = Field(description="이 독자에게 왜 중요한가 한 문장. 요약에 있는 내용만 근거로")
    topic:    str = Field(description="아래 목록 중 정확히 하나만 그대로 쓸 것")
```

### 왜 세 칸으로 나눴나

**칸을 나누는 기준은 "이 칸은 무엇과 대조할 수 있는가" 입니다.**

| 칸 | 대조 대상 | 검수 |
|---|---|---|
| `headline`·`summary` | **원문** | ✅ 검수함 |
| `why` | 없음 (독자 관점의 해석) | ❌ 검수 안 함 |

한 덩어리로 받으면 다음 단계에서 "이 문단이 원문에 근거하는가"를 물을 때
`why` 부분이 매번 불합격합니다. 임계값을 낮추면 진짜 환각도 통과하고요.
**한 덩어리로 받는 순간 그 구분 자체가 불가능해집니다.**

---

## ③ 검수 — 원문 대조 (`graph.py: verify`)

```
아래 요약의 각 주장이 원문에서 뒷받침되는지 판정하세요.
번역이나 단위 환산(예: three months→3개월, $60 million→6000만 달러)은 문제가 아닙니다.
원문에 없는 사실·숫자·과장 표현만 문제로 지적하세요.

[헤드라인] {headline}
[요약]     {summary}

[원문]
{body[:6000]}
```

```python
class Verdict(BaseModel):
    ok: bool = Field(description="요약의 각 주장이 원문에서 뒷받침되면 true")
    problems: list[str] = Field(description="원문에 근거가 없는 대목")
```

### 왜 문자열 대조를 안 쓰나

먼저 숫자 대조를 해 봤습니다. 요약의 숫자가 원문에 있는지 문자열로 세는 방식입니다.
비용이 0이고 결정론적이라 좋아 보였는데, **정확한 요약을 불합격시켰습니다.**

```
원문   just three months after … raised $60 million
요약   지난 3개월 전 6000만 달러 규모의 투자 이후
판정   불합격 — '3' 과 '6000' 이 원문에 없음
```

**번역과 단위 환산을 환각으로 잡은 것**입니다. 검사가 틀린 게 아니라 **대조 대상이 틀렸습니다.**
`"6000"` 과 `"60 million"` 은 문자열로는 다르지만 값으로는 같습니다.
그래서 의미 수준에서 비교하는 LLM 대조로 바꾸고, 프롬프트에 **환산은 문제가 아니라고 못 박았습니다.**

### 실제로 잡아낸 것

```
✗ '일부 연구자들은 AI가 인류를 멸망시킬 가능성을 10% 이상으로 추정' — 원문에 없음
✗ 'Lyft가 상업용 무인택시 서비스에 본격 진출했다' — 원문에 없는 과장
```

---

## ④ G4 적합 관문 (`gate_g4.py`)

교안에 없는 관문을 직접 만들었습니다.

```
아래는 '{소스명}' 매체의 최근 기사 제목입니다.
이 중 **{focus}** 에 해당하는 기사의 번호만 고르세요.
{exclude} 제외합니다.

0. 제19대 비구니 중앙종회의원 후보 10인 선출
1. [부고] 이경숙 전 현대불교 부국장 모친상
...
```

```python
class Fit(BaseModel):
    fit_indexes: list[int] = Field(description="주제에 맞는 기사의 번호만")
    note: str = Field(description="이 매체가 주로 무엇을 다루는지 한 줄")
```

### 왜 키워드 매칭이 아니라 LLM 인가

**선별 노드와 같은 기준을 써야 실제 동작을 예측합니다.** 키워드로 세면
"이 매체에 요가라는 단어가 몇 번 나오나"를 재게 되는데, 우리가 알고 싶은 건
**"이 매체의 기사가 선별을 통과하겠는가"** 입니다. 같은 프롬프트, 같은 기준으로 재야
그 질문에 답이 됩니다.

### 측정 결과가 인상을 뒤집은 사례

| 예상 | 실제 |
|---|---|
| 「수행·신행」 섹션이 가장 적합할 것 | **20% — 탈락** |
| 「기획연재」는 관련 없을 것 | **35% — 채택** (수행·경전 해설 위주였음) |
| 「마음건강 길」은 이름이 딱 맞음 | **10% — 건강 일반지였음** |

**섹션 이름으로 고르면 틀립니다. 재야 압니다.**

---

## 프롬프트를 쓰면서 배운 것 셋

### ① 나열은 AND 로도 읽힙니다

`focus` 에 "…해부학·**과학 연구**" 라고 나열했더니 모델이 그것을 **필수 조건**으로 읽고
실제 수련법 기사를 떨어뜨렸습니다.

```
− 탈락: 15-Minute Seated Yoga Practice — 실제 수련 안내로 과학 연구 내용이 아님
```

**"아래 중 하나라도 해당하면 적합하다"** 로 못 박아 고쳤습니다.

### ② 스키마가 보장하는 건 칸이 채워진다는 것뿐입니다

`topic` 필드 설명에 "모델·도구·산업·정책·연구 중 하나" 라고 적었는데
`"AI, 소프트웨어 개발, 시스템 운영, GPT-6 Astra"` 가 들어왔습니다.

**형식은 스키마가 보장하고, 내용은 검사가 확인해야 합니다.**
`Literal` 타입으로 바꾸고, 그것도 안 되는 자리는 코드로 목록과 대조합니다.

### ③ 프롬프트는 요청이지 보장이 아닙니다

지켜야 하는 규칙은 결국 코드로 셌습니다.

| 프롬프트에 적은 부탁 | 코드로 강제한 것 |
|---|---|
| "같은 사건은 하나만" | `event` 라벨을 세어 중복이면 건너뜀 |
| "한 매체가 독점하지 않게" | `source` 별 카운트로 상한 |
| "반드시 한국어로" | `re.search(r"[가-힣]")` 로 검사 후 재요청 |
| "topic 은 다섯 중 하나" | 목록과 대조해 벗어나면 첫 값으로 |
