[project]
name = "newsletter-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langgraph",
    "langchain",
    "langchain-core",
    "langchain-openai",
    "openai",
    "pydantic",
    "feedparser",
    "trafilatura",
    "requests",
    "python-dotenv",
    "typing-extensions",
    "pyyaml>=6.0.3",
]
